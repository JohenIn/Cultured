<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Cultured" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <!-- Android 12 이상 스플래시 화면 속성 -->
        <item name="android:windowSplashScreenBackground" tools:targetApi="s">@color/black</item>
        <!-- 기본 아이콘 제거 (텍스트를 사용하기 위해) -->
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="s">@null</item>
        <!-- 필요한 경우 아이콘 배경색도 지정 -->
        <item name="android:windowSplashScreenIconBackgroundColor" tools:targetApi="s">@color/black</item>
    </style>
</resources>
